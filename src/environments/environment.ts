// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyCueM8r2dDucDGSwvxnmT5vDE-Byt7nbIU',
    authDomain: 'upt-webb72.firebaseapp.com',
    databaseURL: 'https://upt-webb72.firebaseio.com',
    projectId: 'upt-webb72',
    storageBucket: 'upt-webb72.appspot.com',
    messagingSenderId: '1013952660865',
    appId: '1:1013952660865:web:0646afdce1948c969e5480',
    measurementId: 'G-XVT6K2ERB0'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
